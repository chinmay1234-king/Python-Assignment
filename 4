for i in range(1, 100):       # Loops through numbers from 1 to 99
    if i % 5 == 0:            # Checks if the number is divisible by 5
        continue              # If divisible, skip this iteration
    print(i)                  # Otherwise, print the number
