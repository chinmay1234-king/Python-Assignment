text = input("Enter a string: ").lower()  # Convert to lowercase for uniform comparison

is_palindrome = True

for i in range(len(text) // 2):
    if text[i] != text[-(i + 1)]:
        is_palindrome = False
        print("Not a palindrome.")
        break
else:
    print("It's a palindrome!")
